{
  "name": "vite-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node dev.cjs && vite",
    "build": "node build.cjs && vite build"
  },
  "devDependencies": {
    "sass": "^1.72.0",
    "vite": "^5.1.6"
  }
}
